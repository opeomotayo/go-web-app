apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-go-web-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-go-web-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}